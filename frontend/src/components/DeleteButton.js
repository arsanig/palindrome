
export default function DeleteButton({ messageId }) {

    const handleClick = () => {
    }

    return (
        <button id="delete-button" onClick={() => handleClick()}>
            Delete
        </button>
    )
}
