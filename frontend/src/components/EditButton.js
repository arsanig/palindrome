export default function EditButton({ messageId, message }) {
    const handleClick = () => {}

    return (
        <button id="edit-button" onClick={() => handleClick()}>
            Edit
        </button>
    )
}
