{
    "devDependencies": {
        "concurrently": "^7.0.0",
        "nodemon": "^2.0.15",
        "prettier": "2.5.1"
    },
    "scripts": {
        "dev": "concurrently \"cd ./frontend && npm start\" \"./node_modules/nodemon/bin/nodemon.js ./backend/src/app.js\"",
        "backend": "./node_modules/nodemon/bin/nodemon.js ./backend/src/app.js",
        "test": "echo \"Error: no test specified\" && exit 1",
        "pretty": "npx prettier --write ."
    }
}
